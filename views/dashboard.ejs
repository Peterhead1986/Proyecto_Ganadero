<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/dashboard.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
      /* Sidebar styles */
      .sidebar-menu {
        position: fixed;
        top: 90px;
        left: 0;
        height: calc(100vh - 90px);
        width: 64px;
        background: #222c36;
        border-top-right-radius: 18px;
        border-bottom-right-radius: 18px;
        box-shadow: 2px 0 12px #0002;
        z-index: 100;
        transition: width 0.25s cubic-bezier(.4,2,.6,1);
        overflow-x: hidden;
      }
      .sidebar-menu:hover,
      .sidebar-menu:focus-within {
        width: 260px;
      }
      .sidebar-list {
        list-style: none;
        margin: 0;
        padding: 1.5em 0 0 0;
        display: flex;
        flex-direction: column;
        gap: 0.5em;
      }
      .sidebar-btn {
        display: flex;
        align-items: center;
        gap: 0.7em;
        background: #fff;
        color: #222c36;
        font-weight: 600;
        border: none;
        border-radius: 22px 22px 18px 18px/18px 18px 22px 22px;
        margin: 0.18em 0.4em;
        padding: 0.32em 0.95em;
        font-size: 0.93em;
        min-height: 28px;
        min-width: 0;
        box-shadow: 0 1.5px 6px #0001, 0 1px 0 #fff6 inset;
        cursor: pointer;
        transition: background 0.16s, color 0.16s, box-shadow 0.13s, transform 0.09s, border-radius 0.18s;
        outline: none;
        letter-spacing: 0.01em;
        border-bottom: 2px solid #3cb34a22;
        border-top: none;
        border-left: none;
        border-right: none;
        backdrop-filter: blur(1.5px);
      }
      .sidebar-btn i {
        font-size: 1.05em;
        min-width: 22px;
        text-align: center;
      }
      .sidebar-btn:hover, .sidebar-btn:focus {
        background: #f3f3f3;
        color: #3cb34a;
        transform: translateY(-1.5px) scale(1.045) rotate(-2deg);
        box-shadow: 0 3px 12px #3cb34a22, 0 1.5px 0 #fff6 inset;
        border-radius: 28px 18px 28px 18px/18px 28px 18px 28px;
      }
      .sidebar-btn.sidebar-btn-home { color: #3cb34a; }
      .sidebar-btn.sidebar-btn-listado { color: #3cb34a; }
      .sidebar-btn.sidebar-btn-logout { color: #e74c3c; }
      @media (max-width: 900px) {
        .sidebar-menu { top: 70px; height: calc(100vh - 70px); }
      }
      @media (max-width: 700px) {
        .sidebar-menu { display: none; }
      }
      .dashboard-title-ambientado {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 1.2rem;
        margin-top: 2.5rem;
        margin-bottom: 2.5rem;
        position: relative;
        z-index: 2;
        min-height: 110px;
      }
      .titulo-efecto {
        font-family: 'Montserrat', 'Segoe UI', Arial, sans-serif;
        font-size: 2.8rem;
        font-weight: 900;
        color: #fff;
        letter-spacing: 2px;
        text-shadow:
          0 2px 12px #a8ffb0,
          0 0 40px #baffc9,
          0 1px 0 #222;
        background: linear-gradient(90deg, #fff 60%, #baffc9 100%);
        background-clip: text;
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        position: relative;
        overflow: hidden;
        animation: shine 2.2s cubic-bezier(.4,2,.6,1) 1;
      }
      @keyframes shine {
        0% { background-position: -200px 0; }
        60% { background-position: 200px 0; }
        100% { background-position: 0 0; }
      }
      .icono-ganadero {
        font-size: 2.2rem;
        filter: drop-shadow(0 2px 8px #baffc9);
        animation: icon-pulse 2.5s infinite alternate;
      }
      @keyframes icon-pulse {
        0% { filter: drop-shadow(0 2px 8px #baffc9); }
        100% { filter: drop-shadow(0 4px 16px #a8ffb0); }
      }
      .campo-silueta {
        position: absolute;
        left: 0;
        right: 0;
        bottom: -10px;
        width: 100%;
        height: 80px;
        z-index: 1;
        pointer-events: none;
      }
    </style>
</head>
<body>
    <%- include('partials/_header') %>
    <div class="dashboard-main" style="display: flex;">
      <%- include('partials/_sidebar') %>
      <main style="flex:1;">
        <!-- Aquí va el contenido principal del dashboard -->
        <div class="dashboard-content">
          <% if (flash && flash.success) { %>
            <div class="alert alert-success"><%= flash.success.texto || flash.success %></div>
          <% } %>
          <% if (flash && flash.error) { %>
            <div class="alert alert-error"><%= flash.error.texto || flash.error %></div>
          <% } %>
          <div class="dashboard-title-ambientado">
            <span class="titulo-efecto">Sistema Ganadero Venezolano</span>
            <svg class="campo-silueta" viewBox="0 0 600 80" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M0,60 Q150,20 300,60 T600,60 V80 H0Z" fill="#baffc9" fill-opacity="0.25"/>
              <path d="M0,70 Q200,40 400,70 T600,70 V80 H0Z" fill="#a8ffb0" fill-opacity="0.18"/>
            </svg>
          </div>
          <!-- ...contenido dinámico del dashboard... -->
        </div>
      </main>
    </div>
    <%- include('partials/_footer') %>
</body>
</html>